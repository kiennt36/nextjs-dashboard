import {Inter, Lusitana} from "next/font/google" // import google font

export const inter = Inter({subsets: ["latin"]}) // export Inter font with subsets is latin
export const lusitana = Lusitana({subsets: ["latin"], weight: ["400", "700"]}) // export Lusitana font with subsets is latin and weight is 400, 700